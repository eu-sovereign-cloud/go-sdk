// Package schema provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.4.1 DO NOT EDIT.
package schema

import (
	"time"
)

// Defines values for KubernetesClusterSkuSpecAutomaticUpdatePolicy.
const (
	KubernetesClusterAutomaticUpdatePolicyNone    KubernetesClusterSkuSpecAutomaticUpdatePolicy = "none"
	KubernetesClusterAutomaticUpdatePolicyPreview KubernetesClusterSkuSpecAutomaticUpdatePolicy = "preview"
	KubernetesClusterAutomaticUpdatePolicyStable  KubernetesClusterSkuSpecAutomaticUpdatePolicy = "stable"
)

// Defines values for KubernetesClusterSkuSpecType.
const (
	KubernetesClusterTypeDedicated KubernetesClusterSkuSpecType = "dedicated"
	KubernetesClusterTypeShared    KubernetesClusterSkuSpecType = "shared"
)

// KubernetesClusterSku defines model for KubernetesClusterSku.
type KubernetesClusterSku struct {
	// Annotations User-defined key/value pairs that are mutable and can be used to add annotations.
	// The number of annotations is eventually limited by the CSP.
	Annotations Annotations `json:"annotations,omitempty"`

	// Extensions User-defined key/value pairs that are mutable and can be used to add extensions.
	// Extensions are subject to validation by the CSP, and any value that is not accepted will be rejected during admission.
	Extensions Extensions `json:"extensions,omitempty"`

	// Labels User-defined key/value pairs that are mutable and can be used to
	// organize and categorize resources. They can be used to filter resources.
	// The number of labels is eventually limited by the CSP.
	Labels Labels `json:"labels,omitempty"`

	// Metadata Metadata for SKU resources with name, permission, type, tenant and region information.
	Metadata *SkuResourceMetadata `json:"metadata,omitempty"`

	// Spec Specification of the instance SKU, including its capabilities and extensions.
	Spec *KubernetesClusterSkuSpec `json:"spec,omitempty"`
}

// KubernetesClusterSkuSpec Specification of the instance SKU, including its capabilities and extensions.
type KubernetesClusterSkuSpec struct {
	// AutomaticUpdatePolicy The automatic updates policy for the kubernetes cluster.
	//
	// The policy can be one of the following:
	// - `none`: Automatic updates are disabled for the
	//   kubernetes cluster.
	// - `stable`: Automatic updates are enabled for the
	//   stable version of kubernetes.
	// - `preview`: Automatic updates are enabled for the
	//   preview version of kubernetes.
	AutomaticUpdatePolicy KubernetesClusterSkuSpecAutomaticUpdatePolicy `json:"automaticUpdatePolicy"`

	// EndOfLifeAt The date and time when the version will be end of life.
	// After this date, the version will not be supported. Depending
	// on the CSP this either means no update, more expensive pricing
	// for extended support or a forced upgrade to a newer version.
	EndOfLifeAt time.Time `json:"endOfLifeAt"`

	// ReleasedAt The date and time when the version was released.
	ReleasedAt time.Time `json:"releasedAt"`

	// Type The type of kubernetes cluster.
	// - `dedicated`: A dedicated kubernetes control plane cluster.
	//   This cluster uses extra resources for the control plane.
	//   It is recommended for performance intensive workloads.
	// - `shared`: A shared kubernetes cluster.
	//   This cluster uses shared resources for the control plane.
	//   It is recommended for cost sensitive workloads.
	Type KubernetesClusterSkuSpecType `json:"type"`

	// Version The kubernetes major, minor and patch version.
	// The version is a string in the format of `<major>.<minor>.<patch>` or `<major>.<minor>`.
	Version string `json:"version"`
}

// KubernetesClusterSkuSpecAutomaticUpdatePolicy The automatic updates policy for the kubernetes cluster.
//
// The policy can be one of the following:
//   - `none`: Automatic updates are disabled for the
//     kubernetes cluster.
//   - `stable`: Automatic updates are enabled for the
//     stable version of kubernetes.
//   - `preview`: Automatic updates are enabled for the
//     preview version of kubernetes.
type KubernetesClusterSkuSpecAutomaticUpdatePolicy string

// KubernetesClusterSkuSpecType The type of kubernetes cluster.
//   - `dedicated`: A dedicated kubernetes control plane cluster.
//     This cluster uses extra resources for the control plane.
//     It is recommended for performance intensive workloads.
//   - `shared`: A shared kubernetes cluster.
//     This cluster uses shared resources for the control plane.
//     It is recommended for cost sensitive workloads.
type KubernetesClusterSkuSpecType string
